export struct Profile-info {
    name: string,
    cre-date: string,
    mod-date: string,
    active: bool,
    keys: int,
}

export struct Profile-data {
    info: Profile-info,
    mapping: [string],
}